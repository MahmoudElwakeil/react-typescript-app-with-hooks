export default interface UserViewModel {
    // constructor(_name: string, _title: string) {
    //     this.name = _name;
    //     this.title = _title;
    // }

    name: string;
    title: string;
}